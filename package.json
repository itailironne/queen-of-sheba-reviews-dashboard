{
  "name": "queen-of-sheba-reviews-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Daily-updating BI dashboard of Google reviews for Queen of Sheba Eilat, published via GitHub Pages.",
  "scripts": {
    "scrape": "node scripts/scrape-reviews.js scripts/.last-dump.txt",
    "merge": "node scripts/merge-and-build.js scripts/.last-dump.txt",
    "scrape-booking": "node scripts/scrape-booking-reviews.js scripts/.last-booking-dump.json",
    "merge-booking": "node scripts/merge-booking.js scripts/.last-booking-dump.json",
    "update": "npm run scrape && npm run merge && npm run scrape-booking && npm run merge-booking",
    "stamp": "node scripts/stamp-build.js",
    "test": "node scripts/test.js"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
