# דשבורד ביקורות מלכת שבא — מסמך העברה טכני

## מקור הנתונים
`https://www.google.com/travel/hotels/entity/CgoIytfkyoX724ETEAE/reviews?hl=he&gl=il`
— זה **לא** google.com/maps (שם הביקורות נטענות ב-JS כבד ומבנה ה-DOM אחר), אלא
דף ה-hotel-entity של Google Travel. יש לו כרטיסיית "ביקורות" עם כל הטקסט המלא,
דירוגים לפי תת-קטגוריה (חדרים/שירות/מיקום), "המאפיינים הבולטים" (תגיות), ותגובות
בעלים — הרבה יותר עשיר ממה ש-WebFetch יכול לחלץ מ-HTML סטטי (הדף הזה מרונדר לגמרי
ב-JS, WebFetch מחזיר קליפה ריקה).

## איך הסקרייפינג עובד בפועל (ולמה זה לא טריוויאלי)

### 1. מיון "העדכניות ביותר" — צריך אירועי קלט *אמיתיים*
לדף יש תפריט מיון ("המועילות ביותר" / "העדכניות ביותר" / וכו'). **לחיצת JS רגילה
(`element.click()`) פותחת את התפריט אבל לא בוחרת פריט** — הבחירה בפועל דורשת
אירוע מסונכרן-לחיצה אמיתי. הפתרון שעובד (`scripts/scrape-reviews.js`,
`sortByNewest`):
1. פתיחת התפריט כן עובדת עם JS click רגיל (מטפל delegated על אב משותף).
2. בחירת הפריט השני ("העדכניות ביותר") דורשת `page.keyboard.press('ArrowDown')`
   ואז `Enter` — אירועי מקלדת אמיתיים דרך CDP (`page.keyboard`), לא JS synthetic.
נבדק גם עם `page.mouse.click(x,y)` על קואורדינטות אמיתיות של הפריט בתפריט — גם
זה עבד, אבל הפתרון הסופי משתמש במקלדת כי הוא לא תלוי במדידת bounding-box מדויקת.

### 2. גלילה לטעינת עוד ביקורות — גם כן דורשת אירוע אמיתי
`window.scrollBy()` **לא מפעיל את ה-lazy-load** של הדף (בדוק: הפלט נשאר קבוע
לגמרי גם אחרי הרבה `scrollBy` קריאות). הפתרון: `page.mouse.wheel(0, deltaY)` —
אירועי wheel אמיתיים דרך CDP.

### 3. מגבלת דפדוף — פלאטו קבוע סביב ~30 ביקורות
גם עם wheel אמיתי וסבלנות (חיכינו עד ~10 איטרציות יציבות + ~4 שניות בין כל ניסיון),
הדף **תמיד** מפסיק לטעון ביקורות חדשות אחרי בערך 26-30 ביקורות (כ-9 ימים אחורה
מרגע הריצה). לא נמצא כפתור "טען עוד" חלופי, ולא כתובת URL/API ישירה שעוקפת את זה.
זו כנראה הגבלה מכוונת של גוגל על גישה לא-מחוברת (ייתכן שמשתמש מחובר עם היסטוריה
יראה יותר). **זו לא תקלה שצריך "לתקן"** — העיצוב של כל המערכת (ריצה יומית + דדופ
בהצטברות) בנוי סביב זה מלכתחילה: כל ריצה תופסת רק את מה שגוגל מוכנה להראות, אבל
מכיוון שהריצה היומית תמיד תופסת ביקורות טריות (בתוך יום-יומיים מהפרסום, כי אנחנו
בתוך חלון ה-30), ההיסטוריה **נבנית לבד עם הזמן** בלי שצריך לעולם "להשלים" ביקורות
ישנות שפוספסו.

### 4. הרחבת טקסט חתוך ("… להמשך קריאה")
`expandTruncatedReviews`: מוצא את כל האלמנטים עם הטקסט המדויק "להמשך קריאה"
(תמיד leaf elements בלי ילדים), ולוחץ על כל אחד עם `page.mouse.click` אמיתי
בקואורדינטות ה-bounding-box שלו. גם זה לא עבד עם JS click רגיל.

## פורמט הפלט הגולמי ופרסור (`scripts/lib/parse.js`)

`page.evaluate(() => document.body.innerText)` נותן טקסט קריא-לאדם, לא HTML —
הפרסור מבוסס regex על תבנית עוגן: שורת שם מבקר, ואז מיד אחריה שורה בתבנית
`^(?:תאריך העריכה: )?לפני (.+) באתר  (Google|Tripadvisor)$`. זה עוגן יציב כי
עדכוני CSS/class names של גוגל לא משנים את הטקסט הגלוי. בין עוגן לעוגן:
- ציון (`X/5`) בשורה הראשונה של הבלוק.
- סוג נסיעה אופציונלי (חופשה/עסקים/חברים/נסיעה משפחתית/זוגית) — whitelist קבוע.
- גוף הביקורת — כל שורה שלא תואמת אחד מהדפוסים המבניים (ראה
  `METADATA_PREFIX_RE`: "המאפיינים הבולטים של המלון", תגיות "חדרים.../אוכל
  ושתייה.../פעילויות באזור..." שמודבקות בלי רווח לתוכן).
- תת-דירוגים (`חדריםX.Xשירותx.xמיקוםx.x`, בלי רווחים) — נאספים בנפרד עם regex
  על כל הבלוק, לא תלויים בסדר שורות.
- תגובת בעלים ("תגובה מהבעלים") — מסמן `has_owner_response=true` ומדלג על כל מה
  שאחריו (לא חלק מטקסט הביקורת עצמה).

**אומת ידנית** (2026-09-04) על 30 ביקורות אמיתיות, כולל מקרי קצה: ביקורת עם
אנגלית+עברית מעורבב (Yakir Frank), ביקורת עם רק תת-דירוג "שירות" בלי חדרים/מיקום
(Ofir Zukerman), ביקורות בלי טקסט בכלל (רק כוכבים). כל 30 פורסרו נכון, בלי דליפת
מטא-דאטה לתוך גוף הטקסט.

## תאריך פרסום משוער
גוגל לא חושפת תאריך מדויק — רק טקסט יחסי ("לפני 4 ימים", "לפני שבוע", "לפני
חודש"). `parseRelativeDays()` ממפה את זה למספר ימים (גס ל"שבוע"=7, "חודש"=30
וכו'), ו-`posted_date_estimate = scraped_date - daysAgo`. זה מדויק מאוד לביקורות
טריות (הרוב, כי אנחנו סורקים יומית) ופחות מדויק לביקורות שכבר "לפני שבוע"+ בריצה
הראשונה שתפסה אותן — אבל כיוון שריצה יומית תמיד תופסת אותן כשהן עדיין טריות,
זה לא בעיה מתמשכת.

## דדופ
`reviewId = sha256(reviewer + '|' + text.slice(0,150)).slice(0,16)`. לא תלוי
בתאריך היחסי (שמשתנה בכל ריצה: "לפני יום" הופך ל"לפני יומיים" למחרת) — רק בשם
ובתחילת הטקסט. `merge-and-build.js` טוען את `data/reviews.json` הקיים, בונה
Set של IDs קיימים, ומוסיף רק חדשים. **לא נמחקת/משוכתבת אף ביקורת קיימת אף פעם.**

## תיוג נושאים (themes)
מבוסס מילות-מפתח (`THEME_KEYWORDS` ב-`lib/parse.js`) — 11 נושאים: ניקיון, רעש/מזגן,
צ'ק-אין/אאוט, חיוב/תשלום, בריכה, אוכל, צוות/שירות, מצב חדרים, ילדים/משפחה,
מחיר/תמורה, מיקום. פשוט (substring match), לא ML — מספיק טוב לזיהוי מגמות אבל
לא מושלם (למשל "שקט" תחת noise_ac יכול לתפוס גם ביקורת חיובית על שקט, לא רק
תלונה על רעש — זה בכוונה: בדשבורד אנחנו מפרידים positive/negative לפי rating
הכולל של הביקורת, לא לפי סנטימנט המילה עצמה).

## אזכורי צוות בשם — best-effort, לא מדויק
ניסיון לחלץ שמות עובדים מתוך ביטויי שבח ("תודה ל...", "לציין את...", "בשם...").
**נבדק ונמצא לא-שלם**: מתוך ~5 אזכורי צוות ידועים ידנית בקריאה של 30 הביקורות
(זיקי, איברהים, אמה, חאקים, עומר), הheuristic תפס רק 2 (זיקי, איברהים) — מקרים
כמו "לציין את מנהלת הערב- אמה" (תפקיד בין המילת-רמז לשם) לא נתפסים ע"י regex
פשוט. **הוחלט להשאיר את זה בדשבורד בכל זאת**, אבל ממוסגר במפורש כ"זיהוי אוטומטי,
חלקי" עם משפט הסבר — עדיף רמז חלקי-אך-שקוף מאשר כלום, ועדיף מאשר NER מלא שהוא
overkill לפרויקט הזה. אם ירצו שיפור: אפשר להרחיב את רשימת ביטויי הרמז, אבל תקרת
התשואה נמוכה (הרבה false negatives מגיעים ממבנה משפט חופשי, לא מרשימת ביטויים
חסרה).

## עיצוב הדשבורד (`reviews_dashboard.html`)
- HTML עצמאי לגמרי, בלי embedded data — קורא `data/reviews.json` +
  `data/snapshots.json` ב-`fetch()` בזמן טעינה. **שונה בכוונה** מהמוסכמה של
  `housing-dashboard` (שם ה-data מוטמע בקובץ) — כי כאן ה-dataset גדל כל יום,
  והטמעה הייתה מנפחת את קובץ ה-HTML ומייצרת diff ענק בכל commit. fetch() לקובץ
  JSON באותו origin (GitHub Pages) עובד בלי בעיות CORS.
- כל הגרפים SVG inline, בלי ספריה חיצונית (לא Chart.js הפעם — הפקדים
  המותאמים-אישית שהיה צריך — stacked bar דיברג'נטי ממורכז, sparklines זעירים —
  קלים יותר ל-hand-roll מאשר להתאים Chart.js).
- פלטת הצבעים והספיקים (2px gap, 4px rounded bar ends, קווי גריד hairline וכו')
  לפי סקיל ה-dataviz של Claude; ולידציה עברה עם `validate_palette.js` (8 הצבעים
  הקטגוריאליים + הזוג הדיברג'נטי כחול↔אדום).
- "נפח ביקורות ופילוח דירוג יומי" הוא **stacked bar דיברג'נטי ממורכז**: 5★+4★
  כלפי מעלה מקו-בסיס, 2★+1★ כלפי מטה, 3★ מפוצל חצי-חצי באמצע. זו ההמלצה
  הסטנדרטית ל-ordered-scale share (כמו Likert).
- טאב "נושאים" — small multiples (כרטיס לכל נושא, בהשראת טאב "לפי יישוב" ב-
  housing-dashboard) עם mini-bar דיברג'נטי (כחול=אזכורים מביקורות טובות,
  אדום=מביקורות גרועות) ליום.
- תיקון שנעשה תוך כדי בנייה: תוויות-קצה ישירות על גרף הקו (חדרים/שירות/מיקום)
  התנגשו כשהקווים מתכנסים לערך דומה בסוף הטווח — הוסרו לגמרי (הלג'נד + tooltip
  כבר נותנים זהות וערך מדויק, לפי `marks-and-anatomy.md` §"When end-labels
  collide, don't stack them").

## הריפו וההרשאות
נוצר ב-2026-09-04 עם `gh` CLI (הותקן דרך winget + `gh auth login --web` — ראה
היסטוריית השיחה אם צריך לשחזר תהליך דומה בפרויקט אחר). `gh auth setup-git`
הופעל כדי ש-`git push` הרגיל גם יעבוד בלי לבקש קרדנציאלים כל פעם — בדיוק כמו
ב-housing-dashboard.

## מה עוד לא נבנה / כיווני המשך אפשריים
- אין עדיין toggle יומי/שבועי בגרפים (כרגע תמיד יומי — סביר שיישאר קריא גם
  אחרי חודשים של הצטברות, אבל אם ה-dataset יגדל משמעותית שווה להוסיף אגרגציה
  שבועית כברירת מחדל לטווחי 90+ יום).
- השוואה מול מלונות מתחרים באילת (המלונות "בסביבה" שגוגל מציגה) — לא נאסף.
- אין עדיין alert/notification אוטומטי כשמצטברת סדרת ביקורות שליליות (רעיון
  טבעי להמשך: KPI/badge "3 ביקורות ≤2★ ברצף השבוע" וכו').
